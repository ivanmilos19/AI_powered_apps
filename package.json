{
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "dev": "concurrently \"bun --cwd packages/client dev\" \"cd packages/server && uvicorn app:app --reload --host 0.0.0.0 --port 3000\"",
    "format": "prettier --write .",
    "prepare": "husky"
  }
}
